import localFont from 'next/font/local';

const bebasNeue = localFont({
    src: [{ path: '/public/fonts/BebasNeue-Regular.woff2' }],
    weight: '400',
    display: 'swap',
});

export default bebasNeue;